#main-content
  #align
    .bloc
      .header 
        %h1 Ajout kebab
      .content
        - form_for(@kebab) do |f|
          = render 'form', :f => f
          = f.submit 'Creer'
    .bloc-bottom
#sidebar
  .bloc
    %h2 Actions
    .verybigrow
      = link_to 'Listing kebab', kebabs_path, :class => 'awesome small green'
